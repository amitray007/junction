{
  "minTokens": 50,
  "minLines": 5,
  "//threshold": "Allow a tiny duplication budget (0.5%). The light/dark token blocks in web/src/styles/app.css are intentionally parallel (same token names, different values) — inherent to a tokenized theme, not copy-paste sloppiness. A real duplicate would push well past this on a 200-module codebase and still fail.",
  "threshold": 0.5,
  "reporters": ["console"],
  "ignore": [
    "**/dist/**",
    "**/node_modules/**",
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/drizzle/**",
    "**/*.d.ts",
    "**/migrations/meta/**",
    "**/package.json",
    "**/web/src/server/data.server.ts"
  ]
}
