# Method Files

A **method file** holds one increment's **spec + step-by-step implementation together**, in a single self-contained doc. It is the artifact the orchestrator (Opus) hands to a Sonnet builder subagent — self-contained enough that the builder needs no extra context.

- **Naming:** `NN-<increment>.md` (e.g. `03-cli-boots.md`). Scaffolding uses `00` and `00.5`.
- **The design spec** (`docs/specs/2026-06-22-junction-foundation-design.md`) stays the source of truth; method files are its executable slices. No parallel doc trail.
- **Workflow:** see `docs/workflow.md` for the 8-step loop and the approval gates.

## Increment map

| # | Increment | Method file | Status |
|---|---|---|---|
| 0 | Scaffolding (docs, skills, agents) | `00-scaffolding.md` | written |
| 0.5 | Rules & enforcement (docs/rules + hooks) | `00.5-rules-and-enforcement.md` | written |
| 0.75 | CI + GitHub governance + Changesets | `00.75-ci-governance-versioning.md` | written |
| 0.9 | OSS-ready patterns (AGPL, README, SECURITY…) | `00.9-oss-ready.md` | written |
| 1 | Monorepo skeleton (+ core module structure) | `01-monorepo-skeleton.md` | written |
| 1.5 | Duplication & boundary tooling | `01.5-duplication-tooling.md` | written |
| 2 | core paths + config layer | `02-core-paths-config.md` | written |
| 3 | cli boots over core | `03-cli-boots.md` | written |
| 4 | Data model in core | `04-data-model.md` | written |
| 5 | Persistence (Drizzle + better-sqlite3) | `05-persistence.md` | written |
| 6 | CredentialStore interface + impls | `06-credential-store.md` | written |
| 7 | mcp/server shell | `07-mcp-server-shell.md` | written |
| 8 | Sandbox core | `08-sandbox.md` | written |
| 9 | TUI dashboard (Ink — OpenTUI is Bun-only) | `09-tui-dashboard.md` | written |
| **— Feature: the work-GitHub wedge (any-source broker; GitHub is just the first test) —** | | | |
| 10 | Generic MCP-source create/mutate ops + connection descriptor (Wedge A) | `10-source-create-ops.md` | written |
| 11 | `@junction/mcp-client` — generic upstream connector + debug probe (Wedge B) | `11-mcp-client.md` | written |
| 12 | Profile proxy — agent calls a real tool through a profile (Wedge C) | `12-profile-proxy.md` | written |
| 13 | MCP source management + visibility (remove/enable/disable/show — goal #1) | `13-source-management.md` | written |
| **— Feature: "any source" breadth (OpenAPI/REST → GraphQL) —** | | | |
| 14 | Source-provider abstraction + dispatch-by-kind (OpenAPI prep — refactor) | `14-source-provider-abstraction.md` | written |
| 15 | OpenAPI/REST source provider — call any REST API as namespaced tools | `15-openapi-provider.md` | written |
| 16 | Optional credentials — public/no-auth sources (any source type) | `16-optional-credentials.md` | done |
| 17 | Source-agnostic debug surface — probe + call any source kind | `17-source-agnostic-debug.md` | done |
| 18 | OpenAPI base-URL resolution (relative servers) + early validation | `18-openapi-base-url-resolution.md` | done |
| 19 | Large-spec selection (--tag/--path) + `platform refresh` | `19-large-spec-selection-refresh.md` | done |
| 20 | GraphQL source provider (query/mutation/schema tools) | `20-graphql-provider.md` | done |
| 21 | Sandboxed code-execution source (`cli` kind) + true Seatbelt read confinement | `21-sandboxed-cli-source.md` | done |
| **— Feature: Web UI → connect-once via OAuth → secured via audit (planned route) —** | | | |
| 22 | Web shell + localhost server (read-only dashboard) | `22-web-shell.md` | done |
| 23 | **Web foundation — design system + quality.** Design-led: shadcn-like UI (Radix + Tailwind, owned `ui/` layer), **Geist** type, design tokens (color/spacing/radius/motion, light+dark), base component inventory + status-badge taxonomy, **Emil-Kowalski-grade motion/transitions** (sonner/vaul/View-Transitions, reduced-motion), WCAG-AA a11y, re-skinned dashboard — distinctive, **not AI-slop**. Plus the quality scaffolding: `docs/rules/web.md`, Biome React domain (hooks rules), happy-dom/Testing-Library component harness, `junction-web-reviewer` agent, CI web gate (`vite build` + client-bundle leak-grep). Brief: `docs/design/web-ui-brief.md` → output `docs/design/DESIGN.md` (**decided**). Shipped as Phases A–E + sidebar/stability rework + anti-slop/react-doctor pass (PR #47). | `23-web-foundation-design-system.md` | done |
| 24 | Web: credentials management + **rotation** (core `rotateCredential` + CLI `credential rotate`; first web write-path — assembles on inc-23 form primitives) | `24-web-credentials-mutation.md` | done |
| 24.5 | **Web: UX foundation** — the **setup increment** (decided with user: "inc 25 is the real work"). A **fresh, Geist-grade design system** (rewrites DESIGN.md: dark+light, Geist Sans/Mono, blue accent, single-endpoint model, the route-row signature; retires the inc-23 instrument system) + the 4 surfaces rebuilt on it (Credentials full/wired, Dashboard full, Profiles full read, Platforms lighter) + a **"Coming soon"** affordance for everything deferred (platform/profile mutations, keys/single-endpoint auth, audit). Plan reviewed (1 blocker + token gaps fixed). The ambitious connect-once reframe is a later pre-public-release increment. Shipped PR #55 (Geist-grade system + route-row signature + ComingSoon; Connect-an-Agent kept stdio-honest). | `24.5-web-ux-foundation.md` | done |
| 24.6 | **Web: UX polish** — designer's-eye pass on the 24.5 UI (impeccable:critique, 28→~33/40). Composition fixes: content uses the width (≈1216px, no dead right half); **2-column dashboard** (Connect-an-Agent primary; At-a-Glance + System secondary; real `@media` collapse to 1-col at 48rem); AgentConfig no longer dimmed (full-contrast + dashed preview border, not blanket opacity); dropped the always-empty Platforms "Base URL" column; consolidated repeated ComingSoon chrome to one CLI line per card; minors (route-row arrows, redundant breadcrumb). No new backends. | (folded into `24.5-web-ux-foundation.md` + DESIGN.md decision log) | done |
| 25 | **Web UX v2** — user-driven IA + functionality revision (13 items). Theme→light/dark only; sidebar groups ([Dashboard, Settings]·[Platforms, Profiles, Credentials], no "Manage" label); badge/table/empty-table patterns; dashboard rethink (Connect-an-Agent full-width hero, aligned At-a-Glance stat strip, no card-in-card); **Settings page + real MCP host** (config `mcpHost` + `JUNCTION_MCP_HOST` env, config wins; bracketed IPv6 ok); **Credentials → flat paginated table + group-dividers** (Variant C, search/sort/pagination); **Profiles → master-detail + the first profile write-path** (add/remove/toggle route w/ credential picker + new/delete profile; filter-edit is ComingSoon — no core op); fixed the Credentials ⋯ click bug (opacity-0→always-visible). Pulled inc-26 profile mutations forward. Shipped phased (5 build commits + a review-gate fix commit; 6-reviewer gate caught 2 HIGH correctness bugs + a missing credential picker). | `25-web-ux-v2.md` | done |
| 26 | **Web: platform & route management** — the web write-paths that retire the Platforms + Edit-tool-access "Coming soon" affordances, over one shared `core` extraction. **Blocking core slice:** extract the `platform add`/`refresh` orchestration (`parseSpec`→`extractTools`→`resolveSpecBaseUrl`→assemble, currently inlined in `cli/commands/platform.ts`) into a reusable `core` op, **and** add `{kind:"setFilter", toolFilter}` to the source-ref repo `SourceOp`/`runSourceMutation`. **Leaf slices (fan out):** web **platform add/edit/delete** (all 5 kinds) · web **toolFilter edit-in-place** ("Edit tool access" becomes real) · **cli rewires** onto the extracted core op (no behaviour change). Plans as a **wave** (`docs/methods/_waves.md`). | `26-web-platform-route-management.md` | done |
| 27 | **junction-keys / single-endpoint MCP auth** ⚠️ revises a core invariant. Mint per-profile / global / multi-profile API keys (scoped, revocable, hashed-at-rest); a **single shared MCP endpoint** authenticates by key and the **key selects the profile(s)** — replacing today's unauthenticated per-profile `/profiles/{name}/mcp`. Keys table + **auth middleware in `mcp/server`** + web mint/scope/revoke UI + per-key identity (the attribution audit (31) will consume). When built: update CLAUDE.md + the design spec (the "per-profile endpoints, not shared-endpoint filters" invariant) + the AgentConfig "Shared endpoint" ComingSoon. (Split out of the old inc 26.) Shipped PR #76: core api-keys (mint/verify SHA-256, scoped-proxy arity naming) + `junction serve` HTTP `/mcp` (our own Host/Origin/body guards ahead of the SDK; DNS-rebinding CVE closed) + `junction keys` CLI + web `/keys` UI. Post-merge UI polish: "API Keys" rename, delete-revoked-keys, Key-ID-column removal, SSR date-flicker fix. Invariant retired in CLAUDE.md + the design spec. | `27-junction-keys-single-endpoint.md` | done |
| 28 | **Web: probe + call (in-browser debug surface).** Profile-scoped probe (list a route's tools through the profile — real namespace + toolFilter) + call (invoke one, see the real result) on the Profiles page ⋯ menu. Built as a wave: **Slice A** extracted `buildProvider`/`resolveCredentialSecret`/`makeResolveProvider` into a NEW shared lib **`@junction/source-runtime`** (so web can build a provider without importing cli; `adaptToMcpHandlers` stayed in cli) + a `source-runtime-not-mcp-server` depcruise rule (PR #78); **Slice B** = `probe.server.ts`/`probe.functions.ts` + profiles-page dialogs (PR #79). Real-server QA (agent-browser) caught a green-but-blind bug: buildProvider's lazy `import("@junction/openapi-client")` didn't resolve from web's pnpm-isolated node_modules → empty probe list; fixed by direct web deps + a `web:smoke` runtime-resolution guard. Secret adversarially proven absent from DOM/HAR/SSR/log. | `28-web-probe-call.md` | done |
| 28.9 | **Credential hardening — real kinds + verify-on-add.** Opens the kind gate (**api-key / bearer / env / file**; oauth2 stays gated for 29) via a core **kind↔platform compatibility matrix** (derivation + legacy-bearer acceptance; `addCredential` gained a `platform` param); **verify-on-add / test-connection** with an **honesty matrix** (MCP real listTools · GraphQL `{__typename}` + the previously-missing 401/403→auth-failed mapping · OpenAPI only via operator-designated `verifyOperationId` · CLI honestly not-verifiable), **persisted** (migration 0008) → the reserved badge taxonomy is real (Connected/Auth Failed + pinned-UTC checked captions); **file kind** materialized per-call at 0600 under a junction-private 0700 `<home>/run` dir (NOT shared /tmp — cross-call exposure fix), deleted in a finally on all paths, 32 KiB cap; MCP raw-header auth variant; `credential test` + verified column + web kind-Select/Test-Connection. 4 slices (A locked → B∥C parallel → D), 3 review gates (sandbox-security lead on D), 2 honesty holes fixed (anonymous verify on lost secret; swallowed persistence). PR #82. | `28.9-credential-hardening.md` | done |
| 29 | **OAuth vault (arctic) — connect once.** The big "connect OAuth platforms" expansion. **Slices A1** (pure-core OAuth catalog + `OAuthMetaSchema` refs + `setOAuthTokens` keep-old-merge + oauth2 un-gate + `core-not-http` rule, #86) **→ A2** (refresh engine: `shouldRefresh`/`refreshIfExpired` atomic rotation + single-flight, #88) **→ B∥C∥D** (arctic-backed connect engine + `RefreshTokenFn` + web Connect flow + `junction connect` CLI, #90). Then a **5-task dogfooding follow-up** from real GitHub+Google connect+refresh: (1) `pnpm verify` self-contained — tsc/tsdown dist clobber fixed (#91); (2) **OAuth-native Test Connection** — verifies the TOKEN via the provider userinfo endpoint, not a placeholder source (#92); (3) **refresh-aware badge** — Connected (not a false Expiring) when auto-refreshing, `hasRefreshToken` DTO bool (#93); (4) **reconnect reuses stored client creds** — no re-ask, "use different credentials" escape hatch, partial-swap rejected symmetrically cli+web (#94); (5) **edit credential account label** in place — `renameCredential`, account-label-only (client_id stays a reconnect concern) (#95). arctic 3.7.0. | `29-oauth-vault.md` | done |
| 30 | **App — first-class "connect a service" surface.** Rethink surfaced by the user mid-inc-29: the pre-defined OAuth catalog (google/github/slack/…) was baked into the credential/OAuth layer, and "my GitHub" (one service, many verticals: MCP/OAuth/REST/GraphQL/CLI) had no home in the model. **Design settled with the user + adversarial review (2026-07-04):** add a top-level **App** concept — a new **Apps** sidebar surface + `/app/:id` pages listing your connections to that app (account + the one chosen vertical + status + a lifecycle menu). Grouping is **DERIVED live (G-minus — no schema change, no migration)**, preferring the authoritative `oauthMeta.providerId`; the multi-account **wedge** is preserved (list-of-connections, not a grid). Named **App** (six "Provider" collisions audited; "Provider" stays = the OAuth catalog). Internals (Platforms/Credentials/Profiles) kept reachable behind a bare separator (no eyebrow). New pure `core` **AppCatalog** seeded from **researched real-service data** (≥10 verified real services PER category across oauth/mcp/openapi/graphql/cli — `docs/design/app-catalog-data.md`) proving App ⊥ auth-mechanism. Reuses inc-29 connect/refresh unchanged (byte-identical regression guard). **Design:** `docs/design/provider-concept.md`. Shipped PR #99: core AppCatalog (42 apps) + pure `groupByApp` + `/app` surface. **Real-server QA caught a green-but-blind bug** (dogfooded Google mis-grouped to "Other" — 3 shipped OAuth providers lacked App entries; fixed + a reverse-coverage regression guard + gotchas.md). CI `quality` (jscpd) caught dialog duplication → **deduped** into `components/connection-dialogs.tsx` (not suppressed). CodeRabbit 4 threads resolved. **Slotted BEFORE audit** (user, 2026-07-04). | `30-provider.md` | done |
| 30.5 | **App lifecycle + polish.** The higher-risk / polish work held back from inc 30 (user decision 2026-07-04): **(a) Change method** (swap a connection's vertical, e.g. REST→MCP) as an **additive reconnect-first** flow (connect new + verify + THEN remove old — no stranding) + a helper to reuse the old credential's stored client creds cross-vertical + a security pass; **(b) Test-Connection auto-refresh BUG** — an OAuth credential shows a false "Auth Failed" because Test verifies the expired access token without refreshing first (affects Credentials + Apps; fix = `refreshIfExpired` before verify); **(c) per-app icons/logos** in the Apps surface (self-contained/offline SVG source — evaluate simple-icons; `iconId` on `AppDefinition` + letter-tile fallback). See `30-provider.md` §8. | — | planned |
| 30.7 | **HTTP surface — user-authored REST request-tools.** A new `http` `Platform.kind`: a REST API with no OpenAPI spec junction can ingest, where the OPERATOR hand-authors typed request-tools (name, description, method, path with `{placeholders}`, params each bound to `path`/`query`/`header`/`body`) — the REST twin of the `cli` surface. junction injects the credential into the outbound request + returns the upstream response. **First implementation slice of the App Surface Model** (`docs/design/app-surface-model.md` — surfaces vs auth, schema-backed vs hand-authored families, the catalog-via-importer plan; that design also reframes inc-30.5 "Change method" as *add a surface*, deferring the Slice-3 swap). Built 5 slices (core `HttpConnectionSchema` → `packages/http-client/` provider reusing an extracted `buildAndExecuteRequest` from openapi-client → source-runtime dispatch → CLI `platform add --kind http` → web guided form with the location Select). Reuses OpenApiAuthSchema + the OpenAPI request engine + the CLI tool-authoring model. Shipped PR #104. **Real-artifact QA at every layer** (agent-browser drove the form + a real httpbin.org call; secret injected + swept clean). **Green-but-blind catch:** Slices A–C passed all gates but the DB `platforms` table had no `http` column (silent drop on save) — caught by Slice D's real CLI round-trip, fixed + a "new Platform.kind needs a DB column" checklist in gotchas.md. **Review gate (6 reviewers, no CRIT/HIGH) + CodeRabbit (11 threads)** drove 13 author-time schema refines (hyphenated header names — a real bug; ReDoS-shape guard, narrowed twice to avoid false positives; body-key reservation; baseUrl scheme; path-param required; etc.). SSRF: no agent-exploitable host escape (operator-trust egress = tracked deferred). | `30.7-http-surface.md` | done |
| 30.8 | **App catalog schema + one hand-authored app.** The catalog *shape* proven on ONE real app end-to-end (§4.6 of the App Surface Model). Core: `AppCatalogEntrySchema` (superset of `AppDefinition` — adds `surfaces[]`, `help`, build recipe, verify hints) + a build-time codegen (`gen-catalog.mjs`) reading `catalog/<id>/*.json` → a committed `catalog.generated.ts` (regenerated + git-diff-checked in CI). Rich accessors `getCatalogEntry`/`listCatalogEntries` (surfaces) vs the legacy surfaces-stripped `getApp`/`listApps` (inc-30 back-compat). **GitHub hand-authored** across all 5 surfaces (http empty per the gap-filler rule). Doc-reviewed (5 defects fixed pre-build); build reviews caught orphan-tool-file silent-drop → fail-loud. Shipped PR #108. | `30.8-app-catalog-schema.md` | done |
| 30.9 | **integrations.sh importer (dev-time).** The authoring accelerator: a script reading `GET integrations.sh/api/{domain}/surface` → drafts a catalog entry (fields tagged `detected`/`discovered`) → writes to `.import-staging/` (gitignored, never clobbers live catalog) for human review. Off the runtime path — never a runtime dep. No-fabrication principle (`REVIEW:` sentinels, never invented providerId/specUrl/argv). Correctness reviews caught 2 HIGHs pre-merge (fabricated `bearer` for `mode:"none"`; vacuous oauth2 dedup → silent provider loss). Data-drift proof: the equivalence test derives its expected kind-set FROM the committed fixture, not a doc literal (github.com's live payload lost its mcp surface mid-build). apis.guru top-up + Nango license review split to 30.9.5. Shipped PR #109. | `30.9-integrations-importer.md` | done |
| 30.10 | **Surface-first `/app/:id` page (read-only).** Rebuilds the app-detail page from a flat connections list into a surface-first capability view: per catalog surface, its state (available/connected/serving), auth, catalog details + (for a connected surface) its ACTUAL tools via a NEW platform-scoped `probeSurface` (`buildProvider` direct — handles all 5 kinds, unlike the mcp/openapi-only profile proxy). Pure `intersectSurfaces` core helper ({matched, leftover} — no connection dropped). Fresh metadata-only `SurfaceView` DTO. 3 tool-panel states (list / honest-empty / probe-error). **Fixed a PRE-EXISTING inc-30 routing bug** — `/app/$id` was a child of `/app` (no `<Outlet/>`), so the detail page never rendered (showed the index) on main since inc-30; renamed `app.tsx`→`app.index.tsx` (siblings). **Caught by agent-browser QA** while all 440 tests + smoke + build were green. Reviews (web LEAD/boundary/correctness) clean-to-minor. **CodeRabbit removed from the process** (user, 2026-07-06). Shipped PR #110. | `30.10-surface-first-app-page.md` | done |
| 30.11 | **Catalog-driven one-click connect (token surfaces).** The FIRST increment where the catalog WRITES. "Connect \<app\> \<surface\>" reads the inc-30.8 build recipe (`getCatalogEntry.surfaces[].build`) → pre-fills a confirm dialog → runs the NORMAL validated `addPlatform`/`addCredential` path → **gated by verify** so a wrong catalog guess FAILS and persists NOTHING (verify state is COSMETIC in the serving path — `proxy.ts` filters only on `enabled` — so a red badge would be a false-green; the fix is **verify-before-commit**). Pure `core/build-recipe.ts` `planConnect` (auth mode selects credential kind: token/byo→bearer, ignoring the recipe's oauth2 default; oauth2→deep-link hand-off, inline deferred). `source-runtime/connect-from-catalog.ts` `verifyThenAdd` (verify BEFORE any DB write; auth-failed/unreachable→zero writes) + `confirmThenAdd` (not-verifiable http/cli) + a **platform-id collision guard** (different-kind→refuse, never overwrite). Web: a Connect button + dialog per unconnected surface (account+secret / deep-link / not-verifiable-note; per-outcome inline errors; empty-secret + double-submit guards). GitHub `http` starter tools (proof exception). Built as a 2-slice wave (core+source-runtime locked → web). Doc-reviewed (ce-feasibility + ce-spec-flow → oauth2 deferred, collision guard, guards added pre-build). Review gate: credential-security LEAD / web / boundary all CLEAN; correctness 1 LOW (oauth2-default) + dead-code residual, both fixed. **QA drove the REAL GitHub GraphQL API** (happy path persists; wrong-endpoint→unreachable + bogus-token→auth-failed both persist NOTHING; token swept from DB/store) — and **surfaced a real catalog finding: GitHub's 12.6 MB OpenAPI spec > the 10 MB fetch cap**, so its openapi surface can't connect (recorded in `docs/futures/`; mechanism proven on graphql). Shipped PR #112. | `30.11-catalog-driven-connect.md` | done |
| 30.12 | **Multi-surface connect / add-a-surface (+ multi-account).** Surfaces now ACCUMULATE instead of colliding. GitHub's 5 surfaces switched from `platformIdTemplate: "{app}"` → **`"{app}-{kind}"`** → distinct platform ids (`github-graphql`, `github-mcp`, …); `appIdForConnection` gained a **`<appId>-<kind>` suffix-strip** (step 4, closed build-kind set `{mcp,openapi,graphql,http,cli}` — NOT `custom`; retries ONLY exact-id; runs after exact-id+alias, before displayName so `brave-search` isn't mis-stripped) so accumulated surfaces group back under one `/app/:id`. **Also fixed a latent already-shipped bug**: `github-http` already resolved to `"other"` and vanished from `/app/github`. The App-page Connect button now renders on a CONNECTED surface too (relabeled **"Add account"**) — the multi-account wedge reachable from the app page; add-account writes a 2nd credential via the same-kind collision branch (no platform re-upsert), verify-before-commit inherited. A latent **duplicate-account** gap (no `(platform,account)` unique index; silent double-insert in CLI + `/credentials` + add-account) closed at the ROOT — an app-level `addCredential` typed `duplicate-account` guard (user decision 2026-07-07: app-guard, no risky DB-index migration) wired end-to-end (`ConnectError`→`ConnectFnResult`→account-field UI message, NOT generic "failed"). Built as a 2-slice wave (core+source-runtime locked → web). Doc-reviewed (ce-feasibility + ce-spec-flow caught pre-build: the dup-account error would've collapsed to generic "failed" needing 3-layer wiring; GitHub surfaces are multi-mode `[oauth2,token]` so add-account under oauth2 would discard the typed label; `connect-from-catalog.ts` mislocated to core; legacy-`github` upgrade path uncovered — all folded in). **Review gate: correctness (0 findings) / boundary+credential-security / web / clean-code all CLEAN.** **QA drove the REAL GitHub GraphQL API + the REAL running web server** (agent-browser): 2 distinct surfaces connect + group under `/app/github`; "Add account" renders on the connected surface; dialog opens empty+disabled; dup label → per-outcome account-field message (generic "failed" ABSENT); distinct account persists a 2nd credential, no platform re-upsert; verify-before-commit holds; token absent from DB plaintext. DB unique-index deferred (dedup-then-constrain, `revisit-when.md`). Shipped PR #TBD. | `30.12-multi-surface-connect.md` | done |
| 31 | **Audit (pino) — structured tool-call / credential-use log.** Every tool call routed through junction appends ONE JSONL audit line (WHO: api-key `keyId`+`label` / stdio · WHAT: profile/namespace/tool + arg KEYS + arg HASH · WHEN · OUTCOME: ok/error+kind) to `getPaths().auditLogFile` (`<home>/audit.log`, **mode 0o600**), **NEVER** a secret / arg-value / response-body / error-cause. Hooked at `adaptToMcpHandlers` (`cli/providers.ts`) — the ONE seam both the keyed `/mcp` HTTP path and stdio `mcp serve` converge, with an **arity-aware name parse** (`<profile>__<ns>__<tool>` for multi-profile/global keys vs `<ns>__<tool>`). pino via `pino.destination({sync:false})` (main-thread SonicBoom, NOT a worker transport — avoids dropping the last line), flushed on clean shutdown (SIGINT/SIGTERM/EOF/exit); hard-crash tail-drop documented. Added a distinct **`tool-denied` UpstreamError kind** (user decision) so the audit tells a policy block from a typo, WHILE `safeUpstreamMessage` collapses it to the same opaque agent-facing text (existence-hiding preserved). `junction audit` reads/filters (`--profile`/`--key`/`--tool`/`--since` UTC/`-n`/`--json`). Arg logging = keys+hash never values (user decision); web `/audit` stays ComingSoon (fast-follow). Built as a 3-slice wave (core schema+sink-iface+redact+tool-denied → cli pino-sink+hook ∥ cli audit-read). **Doc-review (ce-feasibility+ce-spec-flow) caught pre-build:** the multi-profile name-parse would've mis-attributed every prefixed line; stdio had no shutdown handler to flush into; overstated arg-hash "not reversible" claim; denial-vs-typo collapse. **Review gate: boundary+clean-code / correctness (0 defects) CLEAN; credential-security LEAD found a real MEDIUM** — `audit.log` was world-readable (0644, pino default) → fixed to 0o600 + regression test (+ a sonic-boom flushSync-not-ready guard + a parseWireName arity table-test). **QA drove BOTH real serving paths** (stdio + keyed HTTP, single + prefixed arity): correct attribution, ok+error, flush-on-exit, `junction audit` reads the real log, token+arg-values swept absent, perms 0o600 on the real path. Shipped PR #TBD. | `31-audit.md` | done |
| 32 | **Security & ops hardening — DECOMPOSED into 32.1–32.5** (like inc 30→30.x; research 2026-07-07 found the load-bearing fact: junction has TWO secret backends — keyring stores secrets directly, encrypted-file wraps them under the master key — so rotation + backup are backend-dependent). User chose the FULL set (32.1 + 32.3 + 32.4 + 32.5; 32.2-heavy-analyzers deferred). **All shipped** — 32.1 (PR merged) + the 32.2-foundation/32.3/32.4/32.5 wave (PR #120). | — | done |
| 32.1 | **CI security quick-wins + serve dir-perms.** `type-coverage ≥99%` gate (source aggregate 99.44%, dedicated `tsconfig.typecoverage.json` include-all incl. mcp/* + web .tsx) + a bash **SPDX-header gate** (`grep -a` for the `data.server.ts` `\x00`-byte file) + **osv-scanner** (CVSS≥7.0, **fail-closed** on unknown/qualitative-only severity + scanner-crash — credential-security review fix) + **`ensureHome()` on `serve`/`mcp serve`** (0700 dir, closing the inc-31 dir-level gap; mcp.ts top-of-run + stderr error path for stdout-purity). All in `security.yml`. Doc-reviewed (ce-feasibility corrected the type-coverage measurement + the include glob + the 1 missing SPDX header). Review: credential-security clean bar the osv fail-open (fixed). QA: ran all gates + drove both real serve paths (0700). Shipped PR #TBD. | `32.1-ci-security-quickwins.md` | done |
| 32.2 | **Shared vault-crypto foundation** (blocking slice for 32.3+32.4). Extracted `gcmEncrypt`/`gcmDecrypt` (generalized-AAD), `deriveKeyFromPassphrase`, `writeFile0600`, `EncFileSchema` into `credentials/vault-crypto.ts` (rule-of-three DRY; scrypt params declared ONCE — the maxmem gotcha) + a NEW `resolveMasterKeyWithTier` (the old `resolveMasterKey` discarded the tier; now delegates to it). Also hoisted the 4 new `CredentialError` kinds (`rotate-refused`/`rotate-failed`/`export-failed`/`import-failed`) + their exhaustive `formatCredentialError` arms here so the two leaves don't edit the same switch in parallel (spec-flow C2). Pure extraction — existing tests byte-identical-green. (NOT the deferred "32.2 heavy-analyzers" — those stay deferred.) Shipped in PR #120. | `32.2-vault-crypto-foundation.md` | done |
| 32.3 | **Master-key rotation** (`junction vault rotate-key`) — backend-aware (file: whole-map re-encrypt, verify-before-swap, pre-rekey backup + fixed-name old-key sidecar + auto-recovery on crash; keyring: refuse+explain). **The recovery model is a SINGLE roll-back rule** (reworked after doc-review found 3 CRITICAL bricking vectors): while the `.master.key.old` sidecar exists a rotation is in flight and recovery ALWAYS restores `{pre-rekey ciphertext, old key}`; deleting the sidecar is the atomic commit — no decrypt-guess branches. Boot hook HOISTED above the keyring/file store-select branch (runs on every open path). env-passphrase rotates (old-salt backed up); env-raw refuses by default or `--print-new-key --i-understand`; empty-vault rotates in place. Rotates within tier (never downgrade posture). 21 crash-injection tests. Doc-review CRITICALs + a builder-found keyring-hijack bug both fixed. Shipped PR #120. | `32.3-master-key-rotation.md` | done |
| 32.4 | **Vault backup/recovery** (`junction vault export/import`) — backend-AGNOSTIC passphrase-wrapped `.jvlt` archive (magic+salt+iv+tag+ct, AAD=header; scrypt→AES-256-GCM). Resolves secrets via `CredentialStore.get` (works both backends → round-trips keyring↔file). oauth2's 4 refs handled via a dedicated validate-before-store.set import path (addCredential refuses oauth2). Collision skip/overwrite/error; profile name-collision handled (no profile upsert exists → skip/error/reject-overwrite); `--out`-in-home refused (bearer secret in the sandbox-visible home); headless no-hang. Master key never in the archive. Doc-review 2 CRITICALs (profile upsert; collision-skip route-drop) fixed pre-build. Shipped PR #120. | `32.4-vault-backup-recovery.md` | done |
| 32.5 | **Tool-poisoning mitigation** — description sanitize at the proxy chokepoint (`proxy.ts` listTools, all source kinds): NFKC + control/format-char strip via a CHAR-CODE LOOP (never a regex — the Biome mangle gotcha) + 2048 cap + drift surfaced via an injected `onDescriptionDrift` callback gated on `strippedSuspicious\|\|truncated` (no cosmetic-drift noise; the tool_call audit schema untouched). `scoped-proxy` (HTTP `/mcp`) inherits sanitized descriptions transitively. Whitespace-acting controls leave a single space (avoids word-gluing — builder-caught). Hash-pinning deferred. Shipped PR #120. | `32.5-tool-poisoning-mitigation.md` | done |
| 33 | Code-mode — QuickJS-WASM over the `ToolProvider` proxy (the fast execution path; "base solid" trigger) | — | planned |
| 34 | **Distribution — publish `junction` + the `junction install` story.** Deliberately the LAST Tier-1 increment (moved from 29, user decision 2026-07-03): **do not publish until the full product is proven working locally end-to-end** — connect-once via OAuth (29) + audit (31) + hardening (32) done and **dogfooded against the real running junction**, not just green gates. Publishing is irreversible-ish (npm name, users depend on it), so it's earned, not scheduled early. Brings the npm publish path + `publint`/`attw` packaging gates + the bin/exports story + a decide-what-ships call (`@junction/web` bundled vs separate). | — | planned (gated on local-proof) |

> **Re-slice note (inc 23+):** a **design-led Web foundation (23)** lands *before* the mutation increments — the design system + the quality scaffolding it's inseparable from — so feature UI (24+) builds on a real, distinctive design system. Then credentials (24) before platforms (25, which needs the `cli → core` spec-parse extraction). **Inc-23 decisions:** design = minimalistic/shadcn-like/Geist/anti-AI-slop (brief: `docs/design/web-ui-brief.md`); component tests = happy-dom + Testing Library; browser dogfooding = gstack `browse` (Vercel agent-browser = optional swappable layer); CI gates = `vite build` + client-bundle leak-grep; skills = `impeccable:*` + `design-consultation` + emilkowalski/skills (motion). Live state + plan: `docs/STATE.md`.

After increment 8 the foundation is "ready"; increment 9 (TUI) completes it. Features come after, each with its own method file.

## Planned route (Tier 1) vs trigger-deferred (Tier 2)

The table above is the **Tier-1 planned sequence** — what we intend to build, in order. It stays lean and goal-directed: Web UI (22–25) → **web platform/route management (26)** → **junction-keys single-endpoint auth (27)** → probe+call (28) → **connect-once via OAuth (29)** → **Provider first-class concept (30)** → **secured via audit (31)** → security/ops hardening (32) → **code-mode (33)** → **distribution (34, LAST)**. **The Provider rethink (30) is slotted before audit** (user decision 2026-07-04) — settle the architecture (does "connect a service" become a first-class entity?) before more features build on the current Credential model. **Distribution is deliberately last and gated on local-proof** (user decision 2026-07-03): junction is not published until the full connect-once → use → audit flow is dogfooded against the real running product. Until then everything runs from the repo (`./junction`). The trigger lives in `docs/futures/revisit-when.md`.

Everything else we've weighed — bwrap egress, per-profile HOME isolation, Seatbelt warm-pool / light mode, microVM tier, sandboxing untrusted MCP binaries, SSRF egress for OpenAPI, GraphQL cost limiting, warm-pool sessions, live config reload, networked mode (Streamable-HTTP + better-auth + AGPL §13), per-field GraphQL tools — is **Tier 2: trigger-deferred**, parked in `docs/futures/revisit-when.md`. Those are NOT scheduled; each activates only when its recorded trigger fires. This is deliberate forward-memory, not a backlog to burn down.

**Cross-cutting decisions baked into increment 22** (so the rest builds cleanly): localhost-only (no auth — single-user); the web is a **management** surface, not a second agent API (MCP stays the agent API); the HTTP server is **callback-ready** so OAuth (26) adds a redirect route rather than a new server; `cli`/`web` are sibling apps (no cross-import — `junction web` spawns the web server; both import `core` only).

## Carry-forward notes (raised in review, actioned at the noted increment)

- **Increment 2+ — `tsc -b` project references:** when `cli`/`web`/`mcp/*` start importing `@junction/core`, each consumer's `tsconfig.json` must add `"references": [{ "path": "../core" }]` (mcp packages: `"../../core"`). Otherwise `tsc -b` won't rebuild `core` first, and the one-way edge isn't machine-checked. (Flagged in the increment-1 boundary + standards reviews.)
