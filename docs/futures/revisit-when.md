# Revisit-when — deliberately deferred decisions

Things we chose **not** to build/adopt yet, each with the **trigger** that should make us reconsider. Deferral is a feature: we keep the foundation lean and add weight only when a real need arrives. When a trigger fires, revisit the row and record the increment that resolved it.

| Deferred | Trigger to revisit | Notes |
|---|---|---|
| **Large-spec catalog connect** (GitHub `openapi` surface blocked by the 10 MB spec cap) | A user needs to connect a catalog `openapi` surface whose spec is >10 MB (GitHub's is 12.6 MB) | inc 30.11 caught this via real-connect QA: `SPEC_BYTE_CAP = 10 MB` (`openapi-client/parse.ts:22`) fires during the streaming fetch, BEFORE `select` can narrow the spec, so GitHub's REST surface fails at assembly (`spec-fetch-failed`). Forward path: raise/stream-window the cap for large specs, a spec-size-aware fallback, or a filtered spec source. Until then GitHub connects via graphql/http/mcp; the connect MECHANISM is proven (graphql, real round-trip). See `gotchas.md`. |
| **Inline catalog-seeded oauth2 connect** (from the App page) | A decision on whether junction ships/brokers an OAuth app (removing BYO client-cred entry), OR the inc-29 `/credentials` registration form gains a catalog-seeded variant | inc 30.11 deferred oauth2-mode connect to a **deep-link hand-off** to `/credentials`: the inc-29 `startConnect` hard-requires BYO `clientId`/`clientSecret`/`scopes` the catalog recipe doesn't carry (only `providerId`), so oauth2 connect can't be one-click. Token-mode connect (the 30.11 payoff) ships fully. |
| ~~**Multi-account via the App-page Connect button**~~ → **RESOLVED inc 30.12** | ~~trigger fired~~ | Done inc 30.12: the Connect button now renders on a CONNECTED surface too, relabeled "Add account" (`app.$id.tsx` B1); add-account writes a 2nd credential through the same-kind collision branch (no platform re-upsert), verify-before-commit inherited. A duplicate `{platformId,account}` is now blocked at the root (`addCredential` `duplicate-account` guard) + a per-outcome UI message. Proven on the real running server. |
| ~~**Platform-id `{app}` collision across an app's flattened surfaces**~~ → **RESOLVED inc 30.12** | ~~trigger fired~~ | Done inc 30.12: GitHub's 5 surfaces now use `platformIdTemplate: "{app}-{kind}"` → distinct platform ids (`github-graphql`, `github-mcp`, …) that ACCUMULATE; `appIdForConnection` gained a `<appId>-<kind>` suffix-strip (step 4, closed build-kind set, exact-id retry) so they group back under `/app/github`. Also fixed the latent bug where `github-http` already vanished to "Other". Proven end-to-end against the real GitHub API. |
| **DB unique index on `(platform_id, profile_name)`** (data-layer dup-account constraint) | A real home is found with duplicate `{platform,account}` labels, OR we want the constraint enforced at the data layer (not just `addCredential`) | inc 30.12 closed the duplicate-account gap at the APPLICATION layer (`addCredential` typed `duplicate-account` guard — protects CLI + `/credentials` + add-account at the root, no migration risk). The DB `credentials` table still has NO `uniqueIndex(platform_id, profile_name)` (only `sourceRefs`/`apiKeys` do). Forward path: a **dedup-then-constrain** migration (a hard `CREATE UNIQUE INDEX` would FAIL on any existing home with dup rows) + map the SQLite UNIQUE violation to the existing `duplicate-account` error. Deferred because the app-guard makes it belt-and-suspenders, and the migration needs the dedup step to be safe. (user decision 2026-07-07) |
| **Effect-TS** (structured concurrency / typed effects) | Concurrent multi-source MCP fan-out, a sandbox process-pool, or cancellation-heavy flows make neverthrow's manual `Result` composition genuinely painful | Today: **neverthrow** is the error model (spec §5a). The exception is narrow — only if structured concurrency earns its keep. Do not adopt for error-handling alone. |
| **Warm-pool / persistent upstream sessions** (`UpstreamSession` reuse) | Per-request connect latency becomes measurable (> ~50 ms p50 on a real workload) or a benchmark shows pool savings outweigh complexity | Today: `createProfileProxy` creates a fresh session per `listTools`/`callTool` call and closes it immediately after (connect-per-call v1, inc 12). Warm-pool would cache a live `UpstreamSession` per (platformId, credentialId) pair and health-check it on reuse. (inc 11, 12) |
| **Sandbox for third-party stdio MCP binaries** | Junction spawns an untrusted or third-party MCP binary via `StdioClientTransport` | Today: the stdio transport spawns the binary without OS-level sandboxing (first-party / trusted sources only). When untrusted binaries are supported, route the spawn through the inc-8 sandbox (`createSandbox`) so the child runs under Seatbelt / bubblewrap. (inc 11) |
| ~~**Long-running daemon** (`junction serve`)~~ → **PARTIALLY RESOLVED inc 27** | ~~trigger fired~~ | Done inc 27: `junction serve` IS a persistent HTTP process (the keyed `/mcp` endpoint). **`core` stays daemon-free** — the daemon lives in cli+mcp/server; core remains embeddable. `mcp serve` (stdio, per-invocation) still exists for direct local wiring. Still deferred: a *networked* daemon (see Streamable HTTP row). |
| ~~**Streamable HTTP transport** (MCP)~~ → **RESOLVED (localhost) inc 27; NETWORKED still deferred** | Remote / **non-loopback** MCP serving arrives | Done inc 27 for **localhost**: `junction serve` uses the SDK's `StreamableHTTPServerTransport` on `127.0.0.1`. The Origin / DNS-rebinding concerns are handled (our own fail-closed Host + Origin guards run before the SDK; `enableDnsRebindingProtection:true`; CVE-2025-66414 — see gotchas). **Still deferred: networked (non-loopback) serving** — that brings TLS, the AGPL §13 network-source-offer (below), and a wider threat model (co-resident users, rate-limiting). SSE stays deprecated/avoided. |
| **better-auth** | Remote / multi-device **web login** is needed | Human web login only; **never** owns platform tokens (those live in the encrypted vault in `core`). |
| **arctic OAuth vault + refresh loop** | Real **OAuth platform sources** connect | The credential store (inc 6) already holds encrypted secrets; arctic adds the OAuth dance + token refresh at that increment. |
| **microsandbox / libkrun microVM** (escalation tier) | Running **hostile code / arbitrary npm**, or needing stronger-than-OS-sandbox isolation | Also the **forward path** for the Seatbelt deprecation (see `deprecations.md`). Drops in behind the existing `Sandbox` interface. |
| **tsgo** (TS7 native compiler) | GA + stable | Swap the `tsc -b` typecheck binary; no code change expected. |
| **Valibot at the web edge** | Web **bundle size** becomes a *measured* constraint | Zod v4 stays in `core` (the boundary validator). Don't pre-optimize. |
| **libsql** (swap from better-sqlite3) | **Whole-DB at-rest encryption** is wanted | Secrets-as-refs (inc 5/6) already neutralizes a DB leak; libsql only if full-DB encryption is independently desired. |
| ~~**pino structured audit log**~~ → **RESOLVED inc 31** | ~~trigger fired~~ | Done inc 31: pino installed (cli only), a JSONL `audit.log` (`getPaths().auditLogFile`, mode 0o600) records every tool call (WHO/WHAT/WHEN/OUTCOME) hooked at `adaptToMcpHandlers` (both transports), `junction audit` reads it. Never logs secrets/arg-values/response-bodies. Proven on both real serving paths. |
| **Audit log rotation / retention** | `audit.log` size / a retention policy is needed | inc 31 ships an UNBOUNDED append-only JSONL file — a broker auditing every call grows without bound. Forward path: size-based rotation (logrotate-style or pino's rotating transport), a `junction audit --prune`, or a max-size cap. Not built (v1 is simple + correct). |
| **Audit: SQLite `audit_events` table** | Indexed audit queries / filtering volume outgrows a JSONL scan | inc 31 stores audit as a JSONL file (no migration, audit-write never blocks the call). A table would buy indexed `--profile`/`--key`/`--since` queries + a natural web read, at the cost of a Drizzle migration + coupling audit writes to the DB. Revisit only if query volume earns it. |
| **Audit: web `/audit` real page + `--audit-args` + listTools auditing** | The CLI read isn't enough / forensic arg-value need / enumeration-auditing need | inc 31 shipped CLI-only read (the `/audit` page stays ComingSoon — a clean independent leaf). Also deferred: `--audit-args` full-value opt-in (v1 logs arg KEYS + a hash, never values — user decision); and auditing `tools/list` enumeration (a `tools_list` event — v1 audits `callTool` only). |
| ~~**Serve entrypoints call `ensureHome()`**~~ → **RESOLVED inc 32.1** | ~~trigger fired~~ | Done inc 32.1: `junction serve` + `junction mcp serve` now call `ensureHome()` (0700 chmod, unconditional so it tightens an existing 0755 dir too) BEFORE `getDatabase`. mcp.ts placement is at the top of `run()` (before the `!profileName` early-return) so ANY serve secures the home; error path uses `process.stderr.write` (stdout-purity). Regression test loosens to 0755 then asserts re-tighten to 0700. Proven on both real serve paths. |
| **AGPL §13 network-source-offer** | The broker **serves network users** | Must surface a source link + embed the running commit/version so network users can obtain source. Wire at the broker/serving increment. |
| ~~**SPDX header CI enforcement**~~ → **RESOLVED inc 32.1** | ~~trigger fired~~ | Done inc 32.1: a bash `spdx-header-guard` job in `security.yml` greps every tracked `*.ts`/`*.tsx` (non-dist) for `SPDX-License-Identifier` — uses **`grep -a`** (force-text) because `data.server.ts` carries a legitimate `\x00` byte that makes plain grep treat it as binary + skip the match (would false-flag a compliant file). Chose the bash gate over `fsfe/reuse-action` (zero new config). Full REUSE/`REUSE.toml` compliance still deferred if separately wanted. |
| **Deferred CI tooling (remaining)** — knip (dead code/deps), targeted semgrep (sandbox/secrets), CodeQL, publint+attw | 32.2 (knip/semgrep, deferred as noisy) · publint/attw when a package first publishes · CodeQL on a public-repo trigger | inc 32.1 landed the quick wins: **type-coverage ≥99%** (source aggregate 99.44%, `tsconfig.typecoverage.json` include-all), **osv-scanner** (CVSS≥7.0 gate, fail-closed on unknown-severity + scanner-crash), **SPDX gate**. trufflehog DROPPED (redundant with the running gitleaks). Spec §5b. |
| ~~**react-doctor**~~ → **ADOPTED inc 23** (on-demand, not in-loop) | ~~trigger fired~~ | Resolved inc 23: adopted as the on-demand React audit (`npx react-doctor@latest --verbose packages/web`), run at the web-increment review step — NOT wired into `pnpm verify`/per-edit hooks (spec §5b). Usage + triage convention in `docs/rules/web.md` (react-doctor section). Baseline taken inc 23 (44 raw diagnostics; true positives fixed in the anti-slop pass). |
| **eslint-plugin-react-hooks** | ESLint is adopted as the project linter (replacing Biome) or Biome drops `useExhaustiveDependencies`/`useHookAtTopLevel` | Biome covers hooks linting (useExhaustiveDependencies, useHookAtTopLevel). ESLint-as-the-loop is banned; adding eslint-plugin-react-hooks would require ESLint. Revisit only if Biome's hooks rules become insufficient. (inc 22) |
| **Tailwind CSS** | The web UI grows beyond the minimal read-only dashboard (mutations, forms, richer data visualization) | inc 22 uses a single `app.css` (~100 lines). Tailwind v4 (CSS-native, no PostCSS dependency) is the preferred path. (inc 22) |
| **Browser e2e tests (Playwright)** | The web UI has mutations or complex interactive flows that need regression coverage | inc 22 is read-only; unit tests cover server data fns. Add Playwright when forms/mutations land in inc 23+. (inc 22) |
| **`ui/index.ts` barrel-import contract** — enforce or soften | A web increment touches enough route imports to make the cleanup cheap, OR a lint rule for it is wanted | The barrel comment says "import from this barrel; never from individual files outside `ui/`", but every route except `credentials.tsx` deep-imports `../ui/<file>.js` (pre-exists on `main`; inc 24.5 rebuilt the routes and reinscribed it). Surfaced by ce-maintainability inc 24.5. Decision: enforce (a Biome no-restricted-import rule + convert the routes) or soften the comment to match reality. Not a regression; deferred to avoid scope creep mid-design-increment. (inc 24.5) |
| **Changesets publishing** | A package is actually **published** | Currently disabled (`private: true` + `changeset tag`); versioning works, publishing is intentionally off. |
| **Credential key rotation** | Operator needs to rotate the master key | The encrypted store supports re-encrypt in principle; no `rotate` command yet. |
| **OpenTUI (Node FFI)** | OpenTUI ships a Node-compatible native renderer | Increment 9 uses **Ink** for the TUI dashboard. OpenTUI's `bun:ffi`-backed renderer throws `"native FFI is not available for this runtime yet"` under Node. When OpenTUI ships a Node-safe renderer, consider migrating the TUI from Ink. See `docs/futures/gotchas.md` (OpenTUI) for the empirical finding. |
| **Live config reload** (source toggles / add / remove take effect on next `mcp serve`) | Mid-session live updates are wanted (e.g. disable a source without reconnecting) | Currently `createProfileProxy` snapshots `enabledSources` once at server construction — a toggle or remove persists to the DB but only takes effect on the next `mcp serve` invocation. MCP clients spawn a fresh serve per session so this is acceptable for v1. When live updates are needed, revisit: re-read `profile.sources` on each `listTools` call, or add a reload signal. (inc 13) |
| **Egress sandboxing for untrusted OpenAPI / HTTP hosts** | Junction calls arbitrary user-supplied `baseUrl` targets | Currently the shared `buildAndExecuteRequest` engine validates the base URL is `https?://` and host-pins it (agent args fill only path/query/body VALUES, never the host — verified: `validatePathValue` + `encodeURIComponent` block `/`,`..`,`%2F`,`@`,scheme escapes; query CRLF percent-encoded; header CRLF undici-rejected; `redirect:"manual"`). But there is **no OS-level egress restriction** — an OPERATOR can point `baseUrl` at `http://localhost` / `169.254.169.254` (metadata). This is **operator-trust** (the operator authors the platform; the AGENT is constrained to values) and acceptable for the single-user self-hosted broker. **inc 30.7 extended this to the `http` surface** — it shares the same engine and inherits the identical deferral (SSRF review confirmed no agent-exploitable path). When untrusted or multi-tenant platform authors are admitted, route BOTH the openapi and http-client `fetch` through the inc-8 sandbox / a shared egress guard to block SSRF to internal hosts. (inc 15; http surface inc 30.7) |
| **Spec complexity guard at add-time** | A spec with a billion-laughs / deep-fan-out `$ref` structure causes `dereference()` to produce a huge in-memory object and an equally huge cache file | Currently `parseSpec` has a 10 MB byte cap on the raw spec download and a 30 s timeout on fetch, but no guard on the dereferenced output size. When spec caching grows or complex nested-$ref specs become common, add an add-time complexity check (e.g. cap the serialised `JSON.stringify(schema)` length or the node-count from `dereference`). Trigger: dereferenced cache files regularly exceed 50 MB or user reports OOM during `platform add`. (raised inc 15) |
| **Full AJV validation of request args against the operation schema** | Agents send systematically bad input (wrong types, missing required fields) and users want structured validation errors instead of upstream 4xx | Currently `callOperation` does light path-injection guards + 4xx passthrough. AJV (or `@cfworkers/ajv`) wired to the `inputSchema` would catch malformed calls before the network hit and return a typed `invalid-args` result. Add at the inc that introduces agent-facing CLI tool-call UX. (inc 15) |
| **Rich platform selection for OpenAPI** (`platform add --kind openapi` selector UX) | More than a handful of OpenAPI sources are being managed and users want a TUI browser | Currently the CLI takes all flags upfront; no interactive tag/operation picker. Wire as a follow-on to the TUI increment (inc 16+). |
| **GraphQL query cost / depth limiting** (`graphql_query`, `graphql_mutation`) | A GraphQL source is connected to an API that has no native cost limiting and agents send complex nested queries causing server-side DoS, or rate-limiting bites | Currently `callGraphQl` enforces `maxQueryBytes` (byte-level guard) but has no per-field depth or complexity budget. When needed, add `graphql-depth-limit` or `graphql-query-complexity` evaluation before `callGraphQl`; gate on a `maxDepth` / `maxComplexity` field in `GraphQlConnection`. (inc 20) |
| **Per-field typed tools for GraphQL** (replace `graphql_query` + `graphql_mutation` with one tool per SDL operation) | Agents reliably have the SDL cached and the MCP client can benefit from a richer, per-operation input schema | Currently the provider exposes 3 fixed tools (query, mutation, schema). When SDL-awareness matures and per-operation schemas add enough value to justify the increased tool count, generate one `Tool` per top-level field on `Query` / `Mutation`. (inc 20) |
| **Extract GraphQL + OpenAPI HTTP executor utilities to `@junction/core`** | A 3rd consumer of the streaming body-read loop, byte-cap logic, or AbortError catch block appears | Currently duplicated (with `jscpd:ignore` markers) between `openapi-client/http.ts` and `graphql-client/http.ts` — deliberate 2nd-use hold until the rule of three fires. When a 3rd package needs these, extract `readBodyCapped`, `isAbortError`, and a shared `callFetch` helper into core. (inc 20) |
| **`credentialEnvVar` denylist refinement** — tighten or expand the `/_TOKEN$|_SECRET$|_KEY$/` schema guard | `validatePolicy`'s denylist evolves (e.g. adds `_PASSWORD` or `_CREDS`) AND `CliConnectionSchema` doesn't mirror it → a name that passes schema validation at `platform add` still gets rejected at run time | Currently the schema `.refine` and `validatePolicy`'s `SECRET_DENYLIST_RE` are intentionally in sync (both reject `_TOKEN/_SECRET/_KEY`). If one changes without the other, users see a confusing "schema-valid but runtime-rejected" error. Keep the two lists in lock-step; they live in `cli-connection.ts` and `sandbox.ts` respectively. (inc 21) |
| **OS-level egress control for the CLI command tier** | Untrusted operators, multi-tenant deployments, or a specific compliance requirement makes uncontrolled network egress by CLI commands a risk | Currently `CliPolicy.allowNet` is declared in the descriptor and validated by `validatePolicy`, but is NOT enforced by the seatbelt/bubblewrap harness for `runCommand` (only `runScript`/Deno enforces `--allow-net`). A CLI command can reach any host unless the host itself firewalls it. Trigger: when host-level firewall is insufficient and per-command egress control is required. Forward path: map `allowNet` → `(deny network*)` + per-entry `(allow network* (remote …))` in the Seatbelt profile; bwrap equivalent. (inc 21) |
| ~~**In-place toolFilter editing for a profile route**~~ → **RESOLVED inc 26** | ~~trigger fired~~ | Done inc 26: `SourceOp` gained `{kind:"setFilter", toolFilter}` (exhaustive switch in `runSourceMutation`), `repositories/profiles.ts` exposes `setSourceFilter`, the web has `setRouteFilterFn` + an in-place allow/deny editor. The Profiles "Edit tool access" ComingSoon is retired. (resolved inc 26) |
| **Web platform edit re-fetches the spec (openapi/graphql)** | Editing an openapi/graphql platform's metadata (e.g. displayName) shouldn't have to re-hit the network, or a partial `platforms.update` op is wanted | inc 26's web `mutateUpdatePlatform` does a **full per-kind rebuild** (existence-checked, then re-run the kind-specific add → upsert). For openapi/graphql that means editing re-fetches/re-introspects the spec on save (correct + no stale state, but heavier than a field patch). A future partial `platforms.update` repo op could patch metadata-only fields without re-fetch. Not needed yet — full rebuild is honest + the common case (mcp/cli) is network-free. (inc 26) |
| **Web platform auth is a bearer-first subset** | A web operator needs the full CLI auth surface (apiKey in query/cookie, basic) when adding openapi/graphql platforms | inc 26's web Add-Platform form exposes `none / bearer / apiKey-in-header` only; the CLI retains full parity (basic, apiKey in query/cookie). Forward path: extend the web auth sub-form when a real need arises. (inc 26) |
| **Code-mode execution layer (QuickJS-WASM over the `ToolProvider` proxy)** | The base (sources + credentials/OAuth + serving) is solid and agents need to *orchestrate* many tool calls cheaply (write code instead of N round-trips) | The planned **fast** execution path, distinct from the OS sandbox (which is for real *local binaries*). The agent writes JS that calls a `tools.*` proxy; the snippet runs **in-process in a QuickJS-WASM isolate** (capability-secure — no ambient FS/net, can only call the proxy; sidesteps the `node:vm`/isolated-vm ban) and each `tools.*` call dispatches to the existing core proxy → providers do the real I/O with injected creds. ~sub-ms/call vs ~25 ms for a sandboxed process. Reuses namespacing + credential injection + toolFilter we already have. Prior art: RhysSullivan/executor (`runtime-quickjs`, `runtime-deno-subprocess`). Build AFTER Web UI → OAuth → Audit. |
| **Per-profile HOME/config isolation for the `cli` source + env-vs-file credential split** | The same CLI tool is used across multiple profiles and must use each profile's OWN credentials/config, not the host's ambient ones | Per-profile credential isolation already comes from the **per-call fresh process + explicit scrubbed env**; the sandbox's FS read-confinement (inc 21) additionally stops file-credential tools (`aws`/`gh`/`git`/`kubectl`/`docker`) from falling back to the host's `~/.aws`/`~/.config` etc. **Two refinements when this is exercised:** (1) auto-provision a per-`(profile,credential)` HOME/config dir (writePath + `HOME=`/`AWS_CONFIG_FILE=` in env) so file-cred tools get profile-scoped state; (2) **env-cred tools** (support `GH_TOKEN`/`AWS_ACCESS_KEY_ID` etc.) need only fresh-process + scrubbed-env (~5 ms) — skip the ~25 ms FS-confining sandbox; reserve the sandbox for tools that insist on credential *files*. |
| **Seatbelt/bwrap per-call overhead (~25 ms) → warm-pool or light isolation mode** | `cli`-source tools are called at high frequency / on-the-fly and the ~25 ms/call spawn+profile-setup tax (measured: raw ~5 ms, sandbox-exec ~21 ms, + junction profile-temp-file) becomes a measured latency problem | The OS sandbox is inherently spawn-heavy; there is no "faster OS jail" (microVMs are *slower* to start). Levers: (a) the fast path for frequent work is **code-mode (in-process, no spawn)** + the HTTP providers (no sandbox) — don't route quick/API actions through the OS sandbox at all; (b) a **light isolation mode** (fresh process + scrubbed env, no Seatbelt) for *trusted env-cred* tools; (c) **warm-pool** persistent sandboxed processes (see the warm-pool row). Do NOT gold-plate the OS sandbox speculatively — it's the niche local-binary path. |
| **Normalize the migration journal (0003 timestamp) + add an old-DB-upgrade / monotonicity regression test** | BEFORE the distribution increment (publishing junction so real users exist), or if any future migration is hand-authored | `0003_add_openapi_column`'s journal `when` (`1782600000000`) is non-monotonic (> 0004/0005/0006), poisoning the drizzle better-sqlite3 high-water mark so later migrations are silently skipped on a DB created in the inc 15–20 window (see gotchas.md). **Today: low-impact** — fresh installs apply all from scratch and future migrations (real, larger timestamps) apply fine; only stale dev DBs are affected (no distributed users). **Before distribution:** (a) lower 0003's journal `when` to between 0002 (`1782426252591`) and 0004 (`1782483347172`) so the chain is monotonic for all new installs; (b) add a regression test — assert `_journal.json` `when` strictly increases by idx, AND seed a raw DB at an earlier migration state, run `getDatabase`/migrate, assert the `graphql`+`cli` columns appear (every existing test used a FRESH DB, which never trips the high-water — that's the gap that hid this). Note: editing the journal does NOT auto-repair already-stuck DBs (their stored `created_at` is unchanged) — fine while there are no real users; if real users ever predate the fix, a one-time `created_at` repair is needed. (raised inc 22) |
| **StatusRail live pulse** (amber flash on active tool call, per-segment activity) | MCP serve is wired to the web process AND a real-time channel (SSE or WebSocket) from inc 26+ | Currently `StatusRail` (`src/ui/status-rail.tsx`) renders static 4-px coloured segments from hardcoded placeholder data. The inc 23 rule: no live data until the real-time channel exists — polling or long-SSE is the trigger. (raised inc 23) |
| **Patch-bay / source-toggle UI** (enable/disable sources from the dashboard) | Write paths (mutations) land in the web UI (inc 26+) | The Profiles page shows read-only source status. Enabling/disabling a source is a write path; the web UI is currently read-only. Wire when mutations are added. (raised inc 23) |
| **Browser e2e tests (Playwright)** — full flow for route navigation + 404 | The web UI has mutations or interactive flows that unit tests can't cover | Currently unit tests cover server functions and component render; route navigation is tested by TanStack's own router tests. Playwright lands when write paths (forms, mutations) appear. (raised inc 22, reaffirmed inc 23) |
| **serve.mjs static-serve regression tests** (traversal guard + leak-grep negative path) | Next time `serve.mjs` or the CI leak-grep is edited, or when the first web test harness can drive the built server | The `resolveStaticFile` path-traversal guard (`startsWith(CLIENT_DIR + sep)`, blocks `../` + sibling-prefix `dist/client-evil`) is verified by manual fuzzing (15 vectors) but has **no automated test** — a future edit could weaken it silently. Add a Vitest harness importing the resolver (or an integration test hitting the running server with `../` paths). Separately, the CI leak-grep's **negative path is unverified end-to-end** — the positive control proves the grep scans content, but nothing asserts the build actually fails when a server-only identifier is planted in a client module. Add a test that plants `@junction/core`/`secretRef` in a client chunk and asserts the gate exits non-zero. (raised inc 23, security re-review) |
| **Cmd+K command palette** (global keyboard-driven nav / action launcher) | Sidebar destination count grows > 8, or a deep-link nav requirement appears (e.g. jump to a specific credential or profile by name without browsing) | Currently `Cmd/Ctrl+B` is the only keyboard global. A command palette (cmdk or similar) would enable jump-to-route, recent-items, and action shortcuts. The inc-23 sidebar already reserves the Cmd+B slot; Cmd+K is the conventional palette binding. (raised inc 23) |
| **Full breadcrumb navigation** (section > entity > detail trail) | Row-detail pages are added (e.g. clicking a credential opens a detail view) | The inc-23 Topbar renders a single-level section name derived from the matched route. A two-level breadcrumb (e.g. "Credentials / github-token") requires route params and loader data — not available until row-detail routes exist. Wire at the increment that adds the first detail route. (raised inc 23) |
| **Origin/SameSite enforcement on web mutation POSTs** | A web **session cookie** is introduced (e.g. better-auth for remote/networked web login) | The inc-24 credential mutation server-fns are POST `createServerFn`s guarded only by the loopback **Host** check (`assertLocalHost` + serve.mjs's 127.0.0.1 bind + HTTP-layer Host guard). Today that is **sufficient** — the endpoints carry no cookies/ambient auth, so there is nothing for a cross-site page to forge (a browser can't spoof a loopback `Host` literal). The moment any web request carries a session cookie, the Host guard alone stops being enough (a same-host malicious page could ride the cookie) → add an **Origin/SameSite** check to every mutation handler. Pairs with the networked-mode / better-auth trigger. (raised inc 24, security review) |
| ~~**MCP-endpoint auth via junction API keys + single shared endpoint**~~ → **RESOLVED inc 27** | ~~trigger fired~~ | Done inc 27: junction mints its own API keys scoped **per-profile / multi-profile / global** (hashed-at-rest SHA-256, display-once, revocable); a **single keyed `/mcp` endpoint** (`junction serve`, localhost) authenticates by key and the key selects the profile(s). Retired the "per-profile endpoints, not shared-endpoint filters" invariant (updated in CLAUDE.md + the foundation spec). Per-key identity (`ResolvedKey.keyId`) is ready for the audit increment (31) to consume for attribution. Tool naming is arity-determined (`<namespace>__<tool>` single, `<profile>__<namespace>__<tool>` multi/global). Eager per-revoke session teardown was NOT built (per-request re-auth is the stronger guarantee); rate-limiting was deliberately not added (localhost + 256-bit secret) — both revisit with networked mode. (resolved inc 27) |
| **Profile rename + multi-profile key tool-name drift** | A profile-rename feature is added (none exists today — repo has create/addSource/get/delete only) | inc 27's multi-profile/global keys embed the **live profile name** in tool names (`<profile>__<namespace>__<tool>`), resolved per session at `initialize`. If rename ever ships, a rename silently changes every multi-profile key's tool names on the next session → breaks agent prompts that hardcoded them. Forward options when rename lands: (a) pin the profile name into the key at mint time (names become immutable per-key), or (b) forbid renaming a profile that any key references. (raised inc 27) |
| **HTTP `/mcp` session-map idle-eviction** (beyond the current count cap) | Long-running `junction serve` sessions accumulate and the 256-session cap is hit in normal use, or memory from idle sessions becomes a measured problem | inc 27 bounds the session map with a hard `MAX_SESSIONS = 256` cap (429 over it) + `requestTimeout`/`headersTimeout`, but has **no idle-TTL sweep** — sessions are reclaimed only on DELETE / transport close / process exit. For a single-user localhost broker with a couple of agents this is ample. When many concurrent/long sessions are real, add an idle-TTL eviction sweep. (raised inc 27) |
| **Schema-driven tool-arg form** (probe/call) | Users find the raw-JSON args textarea error-prone, or a tool's `inputSchema` is rich enough that typed fields materially help | inc 28's web **Call a Tool** dialog takes args as a raw JSON object in a monospace textarea (validated server-side, same as CLI `debug --args`). `ProviderTool.inputSchema` is available, so a JSON-Schema→typed-fields renderer (per-property inputs, nested objects, arrays, required-field validation) is a clean, separable follow-up. Deferred to keep inc 28 shippable. When wanted, build it as a progressive enhancement over the textarea (fall back to raw JSON for schemas the renderer can't express). (raised inc 28) |
| **Whole-profile probe + platform-scoped probe** | A user wants to probe/call an entire profile's toolset at once, OR probe a source in isolation (platform+credential) like CLI `debug` without a profile | inc 28's web probe/call is **per-source, profile-scoped** (a single-source `createProfileProxy`). Two natural extensions: (a) **whole-profile probe** — loop the profile's sources (composes up from the per-source variant); "call" would need a namespace picker across all sources. (b) **platform-scoped probe** — mirror CLI `debug` on the Platforms page (raw upstream tools, derived namespace, no toolFilter). Both deferred; neither has demand yet. (raised inc 28) |
| **Distribution — publish `junction` + `junction install`** (inc 33, gated) | The full **connect-once → use → audit** flow is **proven working locally end-to-end** against the real running junction (OAuth connect (29) + audit (30) + hardening (31) done and dogfooded) — AND the user explicitly decides it's ready to publish | User decision 2026-07-03: Distribution was moved from inc 29 to the **LAST** Tier-1 slot (33) and made **local-proof-gated** — do not publish to the world until the product is complete and dogfooded, not just green-gated (publishing an npm name + having users depend on it is irreversible-ish; earn it). Until the trigger fires, junction runs from the repo (`./junction`) only. When it fires: npm publish path, `publint`+`attw` packaging gates, bin/exports story, and the decide-what-ships call (`@junction/web` bundled into the CLI vs a separate package). (raised inc 28 handover; reslot recorded in the method map + §4) |
| **HTTP Basic auth credential kind + per-account username** | An operator needs Basic-auth against a REST/GraphQL source, OR a Basic-auth platform needs >1 account with different usernames | inc 28.9 shipped api-key/bearer/env/file but **deferred `basic`** (the user's kind list omitted it). The injection is already implemented (`OpenApiAuthSchema` basic → `Authorization: Basic base64(user:pass)`), but the **username lives platform-level** (`openapi-connection.ts` `auth.basic.username`), which breaks the multi-account wedge — two accounts on one Basic platform can't have different usernames. When Basic is wanted: move username to the **credential** (it's metadata, not a secret — a per-account field), add `basic` to the kind matrix, and wire the web/CLI add-forms (web platform-add already defers basic). Pairs with a `basic` verify path (reuse the openapi verify-op mechanism). (raised inc 28.9) |
| **In-place OAuth `client_id` editing** | A user needs to change an oauth2 credential's `client_id` WITHOUT a full reconnect, OR the "swap OAuth app" reconnect path proves too heavy for a common case | Task 5 (inc-29 follow-up) made only the **account label** (`profileName`) editable in place (`renameCredential`), deliberately NOT `client_id`. Rationale: the stored access/refresh tokens are minted against a specific `client_id`; editing it standalone would leave a mismatch (orphaned tokens) until the next reconnect. Changing `client_id` is really "swap OAuth app", which `credential reconnect --client-id` / the web "use different client credentials" flow (Task 4) already handles correctly — it re-mints tokens against the new app. If a lighter standalone client_id edit is ever wanted, it MUST force `needsReauth=true` (so the mismatch is surfaced + a reconnect re-mints), which is essentially reconnect-with-different-creds anyway — so weigh whether it earns a separate path. The secret stays rotate-only regardless (a deliberate boundary). (raised inc 29 Task 5) |
| ~~**Provider as a first-class concept**~~ → **PROMOTED to inc 30** (user decision 2026-07-04) | ~~revisit before/with inc 30~~ — now the *next scheduled increment*, before audit | The user (2026-07-03, mid-inc-29 dogfooding) observed that inc-29's **pre-defined OAuth provider catalog** (google/github/slack/…) is baked *into* the credential/OAuth layer, entangling "generic auth mechanism" with "pre-defined service knowledge." Proposed a separate **Provider** concept: a plugin you "plug in" (pick platform kind + BYO client_id/secret + scopes) that *produces* a generic Credential. Would make the **credential-kind ⊥ platform-kind** orthogonality legible (an OAuth token isn't MCP-bound — works across MCP/OpenAPI/GraphQL/CLI). **Full writeup + open questions + recommendation in `docs/design/provider-concept.md`.** Recommendation: promote the *concept* in the UX (guided "Connect a service" flow + clearer IA) BEFORE committing to a new first-class DB entity or a plugin loader — the catalog already exists and is proven; only add the entity if user-authored providers / cross-platform reuse earn the table. Hold loosely — a genuine architecture call to make WITH the user. (raised inc 29) |
| **Catalog build-recipe `credential.kind:"oauth2"` — how the 30.11 connect interpreter maps it** | The 30.11 connect increment builds the recipe interpreter | inc 30.8's catalog schema lets a surface declare `build.credential.kind:"oauth2"` alongside `auth:[{oauth2,…}]` (both are valid AS DATA — `CredentialKind` has `"oauth2"`, `AppAuth` has `{oauth2,providerId}`) for GitHub's openapi/graphql/mcp surfaces. But `AddOpenApiPlatformInput.auth` / `AddGraphQlPlatformInput.auth` (`AuthInput.scheme`) can only be `apiKey`/`bearer`/`basic` — oauth2 is NOT a settable scheme, it's derived from the spec's `securitySchemes` (`deriveAuthFromSpec`). So the recipe interpreter can't literally "set oauth2" through those add-inputs. The likely resolution (already how inc-29 OAuth-over-REST works today): inject the OAuth access token as a **bearer** credential — the token IS a bearer once minted, regardless of how it was obtained. **Trigger:** the 30.11 connect increment — confirm the recipe interpreter maps `credential.kind:oauth2` → a bearer-injected platform this way, or add a real oauth2 `AuthInput` path if that proves insufficient. (raised inc 30.8, method file §5) |
| **apis.guru spec-URL top-up** (upgrade an importer-drafted `http` REST surface → `openapi` once a real spec is found) + **Nango `providers.yaml` Elastic-License-2.0-vs-AGPL review** (schema reference only until cleared) | inc 30.9.5 (split out of 30.9 to keep the importer focused — see method file §5) — authoring an app whose REST surface has a published OpenAPI spec at apis.guru (CC0), or before ANY Nango data is used beyond reading its schema shape | inc 30.9's importer deliberately defaults every REST surface to `kind:"http"` (empty, no starterTools) and NEVER fabricates a `specUrl` — `AppSurfaceConnectionSchema`'s openapi variant requires `specUrl: z.string().url()`, and a `REVIEW:` placeholder can't satisfy a required URL field. apis.guru (~2,529 APIs, CC0) is the natural top-up source for real spec URLs the integrations.sh payload omits; Nango's `providers.yaml` has the richest OAuth-mechanics reference but ships under Elastic License 2.0 ("no competing hosted service") — do NOT vendor it into junction's AGPL codebase without a license review; reference its schema shape only. (raised inc 30.5 design doc §4.5/§9, reaffirmed inc 30.9 method file §5/§6) |
